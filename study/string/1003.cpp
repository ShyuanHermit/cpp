/*************************************************************************
    > File Name: /home/lvshy/cpp/study/string/1003.cpp
    > Author: lvshy
    > Mail: Shyuan231026nwpu@outlook.com 
    > Created Time: Wed Nov 15 18:55:33 2023
 ************************************************************************/

#include<bits/stdc++.h>

using namespace std;

int main(){
	/*
	这里给出一些伪代码。
	[]-->控制字符；
	如[^\n]就是只识别换行符来停止读取
	当然了，如果是[^c];则遇到c即体制读取。
	那反过来，我们可不可以限制输入的字符呢？又比如按任意键开始，是不是也是一个字符串程序呢？
	答案是肯定的。去掉^就是限制输入。很神奇。我有想法。可以做一个填词游戏。
	当然了，作为礼物，还得有更加优质的图形界面，嘻嘻。
	*/
//下面是所以可能使用到的手段：
	//cin>>str;
	//scanf("%s", &str);
	//printf("%s", str);
	//scanf("%[a-zA-Z0-9]",str);
	//scanf("%[^\n]",str);
	//getline(cin,str1);
	//gets(str);
	//cin.getline(str,10);
	//cin.get(str,11);

return 0;
}
//数组与字符串以及数组的交汇。
//控制字符的学习
